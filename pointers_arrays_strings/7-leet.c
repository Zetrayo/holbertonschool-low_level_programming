#include "main.h"
#include <unistd.h>

/**
 * *leet - Entry point
 * Description: 'leet'
 *
 * @a: pointer pointing to string
 * Return: 0 (Success)
 */

char *leet(char *a)
{
	cheto(a);
	return (a);
}
