#include "main.h"
#include <unistd.h>

/**
 * reset_to_98 - Entry point
 * Description: 'pointer that gives value to an int'
 *
 * @a: number to give
 * Return: 0 (Success)
 */

int reset_to_98(int *a)
{
	*a = 98;
	return (0);
}
